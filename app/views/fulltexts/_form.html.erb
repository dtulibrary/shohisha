<%= form_for(@fulltext) do |f| %>
  <% if @fulltext.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fulltext.errors.count, "error") %> prohibited this fulltext from being saved:</h2>

      <ul>
      <% @fulltext.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
