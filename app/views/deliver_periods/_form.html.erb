<%= form_for(@deliver_period) do |f| %>
  <% if @deliver_period.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deliver_period.errors.count, "error") %> prohibited this deliver_period from being saved:</h2>

      <ul>
      <% @deliver_period.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
