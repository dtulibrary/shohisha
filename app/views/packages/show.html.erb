<p id="notice"><%= notice %></p>

<p>
  <b>Provider:</b>
  <%= link_to @package.provider.description, @package.provider %>
</p>

<p>
  <b>Code:</b>
  <%= @package.code %>
</p>

<p>
  <b>Description:</b>
  <%= @package.description %>
</p>

<p>
  <b><%= link_to "Consumers", consumers_path %>:</b><br />
  <ul>
  <% for consumer_package in @package.consumers_packages %>
    <li>
      <%= link_to consumer_package.consumer.description, consumer_package.consumer %>:
      <%= consumer_package.fulltext.description %>
    </li>
  <% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_package_path(@package) %> |
<%= link_to 'Back', packages_path %>
